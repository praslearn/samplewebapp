apiVersion: v1
kind: Service
metadata:
  name: {{ include "Helmfolder.fullname" . }}
  labels:
    {{- include "Helmfolder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Helmfolder.selectorLabels" . | nindent 4 }}
